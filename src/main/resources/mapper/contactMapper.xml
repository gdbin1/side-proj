<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace="com.gbk.sideproj.mapper.ContactMapper">

  <insert id="insertContact" parameterType="com.gbk.sideproj.domain.Contact">
    INSERT INTO contact (user_id, name, title, content)
    VALUES (#{userId}, #{name}, #{title}, #{content})
  </insert>

  <select id="findAll" resultType="com.gbk.sideproj.domain.Contact">
    SELECT * FROM contact ORDER BY created_at DESC
  </select>

</mapper>